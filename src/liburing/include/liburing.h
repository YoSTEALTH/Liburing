/*
    These are just empty `*.h` files, which will be replaced with actual
    content before compiling.

    This trick is used so `pip` will properly install and uninstall these files.

    Include header files are needed when external library uses `cimport`
    from `liburing` and that library needs header info to compile properly.
*/
